# Generated by Ansible (templates/localconfig.py.j2) -- do not edit by hand.
# Replaces scripts/make_localconfig.py.
import os

hosts = [
{% for h in groups['ramcloud'] %}
    ({{ h | to_json }}, {{ hostvars[h].ansible_host | to_json }}, {{ loop.index }}),
{% endfor %}
]

# Override the upstream config.py default ('-f /dev/sda2,/dev/sdb2') which
# doesn't exist on CloudLab. A regular file in /tmp works fine for a smoke
# test.
default_disks = '-f {{ ramcloud_backup_file }}'
